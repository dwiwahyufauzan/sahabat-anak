# ================================
# SAHABAT ANAK - BACKEND ENVIRONMENT VARIABLES
# ================================
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env to git!

# ================================
# DATABASE CONFIGURATION (Required)
# ================================
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_database_password
DB_NAME=sahabat_anak

# ================================
# SERVER CONFIGURATION
# ================================
PORT=3000
NODE_ENV=development

# Frontend URL for CORS (Required)
# Development: http://localhost:5173
# Production: https://your-domain.com
FRONTEND_URL=http://localhost:5173

# ================================
# JWT CONFIGURATION (Required)
# ================================
# SECURITY WARNING: Must be a strong, random string in production!
# Generate with: openssl rand -base64 64
# Minimum 32 characters required
JWT_SECRET=your-super-secret-jwt-key-change-this-to-something-very-secure-and-random

# ================================
# EMAIL CONFIGURATION (SMTP) (Required)
# ================================
# For Gmail: 
# 1. Enable 2-factor authentication
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the generated password in SMTP_PASS

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# ================================
# NOTES
# ================================
# - All variables marked (Required) must be set
# - In production, ensure:
#   1. JWT_SECRET is a strong random string (min 32 chars)
#   2. FRONTEND_URL uses HTTPS
#   3. Database credentials are secure
#   4. SMTP credentials are valid


